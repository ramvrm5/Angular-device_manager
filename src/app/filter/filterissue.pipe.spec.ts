import { FilterissuePipe } from './filterissue.pipe';

describe('FilterissuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterissuePipe();
    expect(pipe).toBeTruthy();
  });
});
