import { CountDaysPipe } from './count-days.pipe';

describe('CountDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new CountDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
